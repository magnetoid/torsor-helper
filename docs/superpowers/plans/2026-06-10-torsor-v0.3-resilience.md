# torsor-helper v0.3 "Resilience Release" Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline, TDD). Failing test → minimal impl → green → commit. Full suite (`uv run pytest`) green between tasks.

**Goal:** Four research-driven features — impact analysis, an offline slopsquatting guard, temporal-coupling coach recs, and a complexity-trend "new findings only" view.

**Architecture:** Reuses v0.2 reference edges (`symbol_edges`/`who_references`) and the coach pipeline; adds `deps.py`, `coach/coupling.py`, `coach/trend.py`, and one `complexity_snapshot` table. No new required deps.

---

### Task P2: Impact analysis ("who calls this?")

**Files:** Modify `operations.py`, `cli.py`, `server.py`; Test `tests/test_impact.py`.

- [ ] **Step 1 — failing test.** Build a repo where `app.py` does `from pkg.dates import fmt; fmt()` and `cli.py` does `from pkg.dates import fmt; fmt()`; `map_repo`; assert `ops.impact(store, cfg, "fmt")` returns callers including `app.py` and `cli.py` with a total count ≥ 2. Unknown symbol → empty/zero.
- [ ] **Step 2 — fail.**
- [ ] **Step 3 — implement.** `operations.impact(store, config, symbol)`: open index; `syms = db.search_symbols(conn, symbol)`; for exact-name matches collect `dotted = cartographer._norm_module(s.module)`; `callers = db.who_references(conn, dotted, base_name)`; return `{"symbol", "callers": [{"caller","module"}], "count"}`. `cli.py` `impact <symbol>` prints them; `server.py` `impact(symbol)` tool returns a formatted string.
- [ ] **Step 4 — green; full suite.**
- [ ] **Step 5 — commit** `feat(map): impact analysis — who-references blast radius for a symbol`.

### Task P1: Slopsquatting guard (offline dependency-existence check)

**Files:** Create `src/torsor_helper/deps.py`; Modify `operations.py`, `cli.py`, `server.py`, `coach/report.py`, `models.py` (doc kind); Test `tests/test_deps.py`, extend `tests/test_cli_guard.py` or new `tests/test_cli_deps.py`.

- [ ] **Step 1 — failing test.** Fixture repo with a `.venv/lib/python3.11/site-packages/requests-2.0.dist-info/top_level.txt` containing `requests`; a first-party `pkg/` with `__init__.py`; `pyproject.toml` declaring `pyyaml`. A file imports `os` (stdlib), `requests` (installed), `pkg` (first-party), `yaml` (declared via alias), and `totallyfakepkg` (unknown). Assert `deps.unknown_imports(root, [file])` == `["totallyfakepkg"]` (only the hallucinated one). Unit-test the helpers: `stdlib_names()` contains `os`; `installed_import_names(root)` contains `requests`; `first_party_names(root)` contains `pkg`; `declared_import_names(root)` maps `pyyaml`→`yaml`.
- [ ] **Step 2 — fail.**
- [ ] **Step 3 — implement.** `deps.py` with `stdlib_names()`, `installed_import_names(root)` (scan `.venv`/`venv` site-packages `*.dist-info/top_level.txt` + `*.egg-info`), `first_party_names(root)`, `declared_import_names(root)` (+ `_ALIASES` table), and `unknown_imports(root, files)` (parse top-level absolute imports via `ast`, flag names in none of the known sets; skip relative imports). `operations.check_dependencies(store, config, files=None)` (defaults to `_git_changed`) → list of `{"file","line","name"}`; `cli.py` `deps [files] [--strict]`; `server.py` `check_dependencies(files?)` tool; `coach/report.assemble` appends `kind="phantom_dep"` recs (severity `info`) when conn present.
- [ ] **Step 4 — green; full suite.**
- [ ] **Step 5 — commit** `feat(deps): offline slopsquatting guard — flag imports that resolve to no known package`.

### Task P3: Temporal-coupling coach recommendations

**Files:** Create `src/torsor_helper/coach/coupling.py`; Modify `coach/report.py`, `models.py` (doc kind); Test `tests/test_coach_coupling.py` (extend `git_project`).

- [ ] **Step 1 — failing test.** Extend a git repo: commit `a.py`+`b.py` together 3×, `c.py` alone. `coupling.find_coupling(root)` returns the `(a.py, b.py)` pair with degree ≥ 0.6; `c.py` not coupled. Non-git → `[]`. `report.assemble` (conn present, git) includes a `kind="coupling"` rec naming a.py & b.py (since neither imports the other).
- [ ] **Step 2 — fail.**
- [ ] **Step 3 — implement.** `coupling.py`: `_is_git_repo`/git-log reuse (import from `coach/hotspots` or duplicate the guarded helper); `find_coupling(root, min_commits=3, max_files=40, threshold=0.6)` → list of `(file_a, file_b, co_changes, degree)`. `find_coupling_recs(root, conn)` filters to pairs not linked by `symbol_edges` (module_edges) → `Recommendation(kind="coupling", severity="suggest", key=f"coupling:{a}:{b}", score=degree*co)`. `report.assemble` appends when conn present.
- [ ] **Step 4 — green; full suite.**
- [ ] **Step 5 — commit** `feat(coach): temporal-coupling recommendations from git co-change`.

### Task P6: Complexity-trend "new findings only" view

**Files:** Create `src/torsor_helper/coach/trend.py`; Modify `db.py` (table + helpers, SCHEMA_VERSION→4), `coach/report.py`, `operations.py` (`consolidate` snapshot), `models.py` (doc kind); Test `tests/test_coach_trend.py`, extend `tests/test_db_symbols.py`.

- [ ] **Step 1 — failing test.** `db.save_complexity_snapshot(conn, {"h.py": 10})`; `load_complexity_snapshot(conn) == {"h.py": 10}`. `trend.find_regressions` with current `{"h.py": 30}` vs snapshot `{"h.py": 10}` → a `kind="regression"` rec for `h.py`; unchanged `{"h.py": 10}` → none; a brand-new complex file → rec. `consolidate` writes a snapshot (assert the table is populated after).
- [ ] **Step 2 — fail.**
- [ ] **Step 3 — implement.** `db.py`: `SCHEMA_VERSION=4`; `complexity_snapshot(file PRIMARY KEY, complexity INTEGER)`; `save_complexity_snapshot(conn, mapping)` (replace-all); `load_complexity_snapshot(conn)`. `trend.py`: `current_complexity(root)` (reuse `hotspots._complexity` over `iter_source_files`); `find_regressions(root, conn, rel=0.25, abs_=5)` → `Recommendation(kind="regression", severity="suggest", key=f"regression:{file}", score=delta)`. `operations.consolidate` computes `current_complexity` and `db.save_complexity_snapshot`. `report.assemble` appends `find_regressions` when conn present.
- [ ] **Step 4 — green; full suite.**
- [ ] **Step 5 — commit** `feat(coach): complexity-trend regressions (new-findings-only) + consolidate snapshot`.

---

## Final integration
- [ ] Adversarial / focused review of the branch diff; fix confirmed findings.
- [ ] Dogfood ADR(s) for the load-bearing v0.3 decisions (e.g. "dependency check is advisory and best-effort; venv is ground truth").
- [ ] README (capabilities, new CLI/MCP surface: `deps`, `impact`, coupling/regression recs), CHANGELOG `[0.3.0]`, bump `__version__`→`0.3.0`.
- [ ] `uv run pytest` green + `uv run ruff check` clean.
- [ ] finishing-a-development-branch → merge to `main`, push.

## Self-review notes
- `Recommendation.kind` gains `phantom_dep`, `coupling`, `regression` — documented in `models.py`, ranked by the existing `_SEVERITY_RANK` in `report.assemble`.
- All git-dependent features (coupling, trend-via-consolidate uses no git) reuse the `_is_git_repo` guard so non-git/offline runs are quiet.
- `complexity_snapshot` is the only schema change (additive, SCHEMA_VERSION→4).
- Determinism: degree/regression are pure functions of git history + stored snapshot; no clock.
