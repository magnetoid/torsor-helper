---
paths:
  - "src/torsor_helper/hooks.py"
---

# ADR 0012: The edit gate is advisory by default and checks proposed content

- forbid_import: `torsor_helper.operations` in `src/torsor_helper/hooks.py` — hooks.py is the pure hook core (ADR 0012) — verdict logic (pre_edit, session_start_context) lives in operations, never the reverse
- forbid_import: `torsor_helper.guard` in `src/torsor_helper/hooks.py` — hooks.py only templates and merges hook entries (ADR 0012) — it never evaluates rules itself

Machine-enforced — `torsor guard` flags violations.

<!-- generated by `torsor rules --scoped` from .torsor/architecture/decisions/0012-the-edit-gate-is-advisory-by-default-and-checks-proposed-content.md; edit the ADR, not this file -->
