---
paths:
  - "src/torsor_helper/operations.py"
---

# ADR 0011: Cleanup is plan-then-apply, and never touches a stable tier

- forbid_pattern: `shutil\.rmtree` in `src/torsor_helper/operations.py` — recursive deletion belongs in cleaner.py (ADR 0011) — operations.py orchestrates a plan, it never removes trees itself

Machine-enforced — `torsor guard` flags violations.

<!-- generated by `torsor rules --scoped` from .torsor/architecture/decisions/0011-cleanup-is-plan-then-apply-and-never-touches-a-stable-tier.md; edit the ADR, not this file -->
