---
type: map
status: derived
tags:
- map
links: []
created: '2026-09-04T05:04:01'
updated: '2026-09-04T05:04:01'
---

# src/torsor_helper/clients.py

Symbols in `src/torsor_helper/clients.py`.

- L67 `config_location(client: str)` (function) — Where the snippet from config_snippet() should be pasted, per client.
- L72 `instructions_file(client: str)` (function) — The conventional agent-instructions filename for a client (AGENTS.md by
- L78 `mcp_servers_block(root: str)` (function) — The standard `mcpServers` JSON block that registers torsor-helper.
- L87 `config_snippet(client: str, root: str)` (function)
- L139 `write_mcp_json(project_root, server_root: str)` (function) — Write/merge a project `.mcp.json` registering torsor-helper, preserving
