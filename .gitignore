# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs / uv
.venv/
# uv.lock IS committed: torsor is an application, and every runtime dep
# except mcp is unbounded, so an unpinned resolve can break a fresh
# install with no code change.

# Test / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store

# Subagent-driven-development scratch (ledgers, briefs, review packages)
.superpowers/

# Local git worktrees (Claude Code)
.claude/worktrees/
